These files can just be run in Python if you have Python PDAL installed.

Just make sure to change the input and output file names in the PDAL pipeline (the text block that is named "json").

The files should be combined, which can be done if there is time left.
